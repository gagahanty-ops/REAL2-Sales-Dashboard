---
tags: [real2, current-status, priorities]
date: 2026-09-17
---

# Текущие приоритеты и точка продолжения

## Текущее состояние

Ветка: `feat/foundation-access` в `https://github.com/sarrinoj-glitch/REAL2-Sales-Dashboard.git`.

Рабочая копия на машине `ishop`: `/Users/ishop/Desktop/REAL2-Sales-Dashboard.nosync` (`~/Desktop/REAL2-Sales-Dashboard` — симлинк на неё). Суффикс `.nosync` обязателен: рабочий стол синхронизируется с iCloud, и при почти полном диске macOS выгружает файлы репозитория и `node_modules` в облако (`dataless`), после чего lint/тесты падают случайным образом.

Последние коммиты Task 2:

- `23e4853` — `feat: normalize REAL2 lead snapshots deterministically`;
- `0072eff` — `fix: harden deterministic lead normalization`;
- `4c46499` — `fix: mask phones with any common separator in lead names`;
- следующий за ними docs-коммит с этим чекпоинтом (точный hash — `git log -1` на ветке).

## Что завершено

- Plan 1 foundation/access.
- Plan 2 amoCRM read-only sync (Tasks 1–7).
- Plan 3 Task 1 — нормализованная схема, полный gate пройден.
- Plan 3 Task 2 — детерминированная нормализация: московская дата, точные рубли без float, каналы только по активным точным правилам, `normalizeLead` с явными `normalized/excluded/rejected` и quality-кандидатами, маскирование телефонов в `displayName`. Независимое ревью: после двух fix rounds оба вердикта APPROVED, открытых замечаний нет.

## Что НЕ закрыто — блокер окружения

Полный controller gate Task 2 выполнен частично. На машине `ishop` нет Docker и Supabase CLI, поэтому не запускаются `supabase db reset` и DB-зависимые тесты (86 integration, 19 из 24 security, 16 unit в `oauth.test.ts`).

- Всё, что не требует БД, прошло на Node 22: lint, typecheck, build, contracts, secret scan, diff check, repo/worker 16/16, unit 317 (все файлы без БД), security 5, integration 12.
- Task 2 не меняла SQL, репозитории, интеграции, worker и web.

Почему не удалось поставить окружение:

- brew отказывается устанавливать пакеты, пока не обновлены Xcode Command Line Tools (нужен `sudo` владельца);
- свободно около 2 ГБ диска;
- swap исчерпан (8 ГБ RAM).

## Что делать следующим

1. На машине с Docker и Supabase CLI (или после того, как владелец освободит диск и память и обновит CLT) выполнить полный gate на HEAD ветки.
2. Если gate зелёный — записать результат в `raw-context/sdd/2026-09-12-03-normalization-metrics/progress.md` и начать Plan 3 Task 3 (история этапов и milestones).
3. До этого Task 3 не начинать.

## Открытый вопрос владельцу

`METRICS_CATALOG.md` §7 / `SPEC.md` M3.5 стоит явно дописать двумя уточнениями:

- заполненное, но несопоставленное поле «Источник сделки» даёт `unknown` — теги в этом случае не используются (решение R1 подтверждено ревью);
- конфликт каналов оценивается внутри одного вида источника.

## Сколько осталось

Из 33 задач дорожной карты код готов для 15, но Task 2 ждёт DB-части gate. Осталось 18 задач: Plan 3 Tasks 3–7, Plan 4, Plan 5.

## Безопасность

- Live OAuth, amoCRM, Google API и исходная таблица не затрагивались.
- `SYNC_ENABLED=false` и `SHEET_PUBLISH_ENABLED=false`; локального `.env` на машине `ishop` нет.
