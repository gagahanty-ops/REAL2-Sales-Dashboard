# SDD ledger — plan: docs/superpowers/plans/2026-09-12-02-amo-readonly-sync.md

## Binding context

- Spec authority: `SPEC.md` modules M2–M4 and `SECURITY_READ_ONLY.md` sections 3–4 and 8–11.
- Branch/workspace: `feat/foundation-access` in `/Users/arlandorizzi/Desktop/REAL2-Sales-Dashboard`.
- Production side effects forbidden in this implementation session: no live OAuth installation, no real amoCRM credentials or requests, no Google API access, and no write to protected spreadsheet `123QVhKGG3Y6ZlyHYnuKG_1BPhS82FcYaqY96nsB7Iks`.
- Test inputs are synthetic; both sync switches remain disabled.

## Preflight cross-task scan

| Tasks | Producer / consumer or shared surface | Finding / ruling |
|---|---|---|
| 1 → 2 | Task 1 owns the sole amoCRM transport; Task 2 uses its exact OAuth token POST. Both touch `packages/integrations/src/amo`. | OAuth requests use the guarded transport with kind `oauth`, receive no bearer token, and never log body/query values. |
| 1 → 3 | Task 3 callback must verify the account through Task 1 GET `/api/v4/account`. | Account verification must use `amoFetch`; no route-level/global fetch. |
| 1 → 4 | Task 4 discovery consumes the Task 1 GET allowlist. | All discovery endpoints go through `amoFetch`; no generic proxy or dynamic path outside the closed policy. |
| 1 → 5 | Task 1 emits safe audit entries; Task 5 persists `amo_api_audit`. | Persist only method, normalized path, status, duration, trace ID, attempt, and result—never URL query/body/token. |
| 1 → 6 | Task 6 pagination/retry consumes the guarded transport. | Retries may repeat only already-allowed requests; URL following must be revalidated on every page/redirect candidate. |
| 1 → 7 | Task 7 statically proves Task 1 remains the only production amoCRM network boundary. | Scanner must fail closed on alternate production clients without creating broad exceptions. |
| 2 → 3 | Task 2 produces OAuth state/token/connection functions; Task 3 exposes admin-only routes. | Routes return safe projections only; code, secret, token pair, and raw OAuth response stay server-side. |
| 2 → 4 | Task 4 configuration references Task 2 `amo_connections` and consumes its token provider. | Configuration requires an active, exact-account connection and stores no token material. |
| 2 → 5 | Task 5 sync runs reference `amo_connections`. | Migrations remain ordered (`0002` before `0004`) and all FK targets are immutable identifiers. |
| 2 → 6 | Both add worker jobs; Task 6 consumes refresh/connection behavior from Task 2. | A 401 permits exactly one locked refresh and one retry; disabled/reauth-required connections stop before business calls. |
| 2 → 7 | Task 7 scans OAuth/network code and documents external installation. | Static checks allow only exact token POST; runbook requires external OAuth and written support confirmation. |
| 3 → 4 | Both expose admin settings/API surfaces and reuse role checks/envelopes. | Preserve existing `requireRole` and safe response envelope; no client-side secrets. |
| 3 → 5 | No direct file ownership; both consume the connection identity/audit contract. | No conflict found; connection IDs, not credentials, cross the boundary. |
| 3 → 6 | Task 3 can disconnect/refresh while Task 6 syncs. | Sync rechecks active connection and fails safely; disconnect does not call amoCRM business APIs. |
| 3 → 7 | Task 7 reviews routes/runbook for credential exposure. | Redirects, bodies, HTML, and logs must be covered by redaction/security tests. |
| 4 → 5 | Task 5 `sync_runs.config_id` consumes immutable Task 4 versions. | Never mutate historical config rows; a run pins one exact config version. |
| 4 → 6 | Task 6 consumes active config and may trigger recalculation after activation. | A candidate activates only after live-discovery checksum validation; current snapshot stays unchanged until later quality gates pass. |
| 4 → 7 | Task 7 covers paths/imports used by discovery/config routes. | Screenshot-derived IDs stay fixtures only and cannot be accepted without API-confirmed discovery. |
| 5 → 6 | Task 6 writes through Task 5 raw/sync repositories and advances cursors. | Raw writes remain append-only; cursor replacement occurs only in the same successful finish transaction. |
| 5 → 7 | Task 7 checks the audit/request ledger and safety invariants. | Security proof must include zero forbidden calls and no query/body values in audit data. |
| 6 → 7 | Task 6 creates mock fixtures/request ledger consumed by Task 7. | Test-only servers/fixtures are not production network clients; any scanner exception must be exact-file and test-only. |

## Preflight task self-consistency scan

| Task | Tests vs code; created vs later-touched files | Finding / ruling |
|---|---|---|
| 1 | Policy tests cover exact allowlist and pre-network denial; transport test covers injected fetch, redirects, and schema validation. | Consistent. Normalize away query values in audit, but preserve query parameters for the actual allowed GET request. |
| 2 | Tests require state replay/expiry, encryption, rotation, concurrency, and redaction; migration/repository/worker surfaces provide them. | Consistent with one boundary condition: OAuth POST uses Task 1 transport and may carry a request body, but the body is never audited/logged. |
| 3 | Route test covers role, state, wrong account, refresh, disconnect, and leakage; listed routes/UI consume Task 2. | Consistent. Callback remains GET in this app while only its server-side exchange is POST to amoCRM. |
| 4 | Domain and integration tests cover discovered IDs, checksum, atomic activation, channel rule uniqueness, and roles. | Candidate numbers in the plan are synthetic fixtures, not approved production IDs. |
| 5 | Integration test covers dedupe, immutability, partial cursor rollback, audit shape, RLS; migration/repositories implement the journal. | Consistent. Retention is the sole controlled deletion path and must not grant ordinary application roles UPDATE/DELETE. |
| 6 | Worker tests cover double switch, lock, complete/partial runs, retry schedule, pagination loops, overlap, watchdog, retention, and count drop. | Consistent, but the plan pseudocode is illustrative: implementation must inject dependencies and keep zero token/network reads before both switches. |
| 7 | Static tests and runbooks prove a single transport, exact methods, disabled switches, support-safe external OAuth, and shutdown. | Consistent. No production installation or credential is part of the completion gate. |

## Preflight rulings

- Ruling: use PostgreSQL 17, not the plan header's PostgreSQL 16 — `SPEC.md` 0.2 and the already-implemented Supabase foundation are authoritative — if wrong, migrations may need compatibility edits for a PostgreSQL 16 deployment.
- Ruling: all plan IDs other than the confirmed host and protected spreadsheet ID are synthetic candidates until discovered through the external API — M3 requires live API confirmation and rejects screenshot-derived assumptions — if wrong, production mapping will remain intentionally blocked until an owner validates it.
- Ruling: an OAuth token POST is the only permitted non-GET amoCRM request and must traverse the same policy before token lookup or network; it receives no bearer token — the security contract allows exactly this exchange — if wrong, OAuth connection tests will fail closed rather than broaden business access.
- Ruling: append-only means ordinary app/user roles get no UPDATE/DELETE grants or policies, while the 90-day retention path is a separately scoped service operation that first proves hashes/normalized history exist — this reconciles M4 retention with raw immutability — if wrong, retention remains disabled rather than risk deleting audit evidence.
- Ruling: Task 7 may recognize exact test-only mock-server/fixture files but may not allow a second production HTTP client — test infrastructure must exist without weakening the single-transport invariant — if wrong, the static gate will produce false positives or need a narrower parser.

## Task progress

- Task 1: dispatched to `/root/plan2_task1_impl` from base `6c43c702a445a70978c347b11605f15f4997ee49`; brief `task-1-brief.md`; report `task-1-report.md`.
- Task 1: implementation commit `70121b92cc64ba68aeef17bf8b580643ab53924d`; review dispatched to `/root/plan2_task1_review` with `review-6c43c70..70121b9.diff`.
- Task 1: review reported an Important finding that `result` exceeds the audit field list, plus two minors concerning explicit default `:443` normalization and malformed-header audit coverage.
- Task 1: Ruling: retain closed-enum audit `result` — authoritative `SPEC.md` M4.2 defines `amo_api_audit.result`, Plan Task 5 consumes it, and its fixed values carry no query/body/token/PII — if wrong, the audit schema and Task 1 type will need a coordinated removal before Task 5.
- Task 1: minor (deferred): WHATWG normalizes an explicit default `:443`; the implementation denies non-default ports but the report overstates rejection of every explicit port spelling.
- Task 1: minor (deferred): malformed caller header construction can throw after policy approval but before the transport's audited try/catch.
- Task 1: complete (commits `6c43c70..70121b9`, Important resolved by Ruling; 2 minors deferred).
- Task 1: controller verification on exact Node `v22.23.2`: workspace lint PASS; typecheck PASS; repository/worker tests 10/10 PASS; unit tests 41/41 PASS; focused policy/transport tests 22/22 PASS; security tests 10/10 PASS.
- Task 2: dispatched to `/root/plan2_task2_impl` from base `70121b92cc64ba68aeef17bf8b580643ab53924d`; brief `task-2-brief.md`; report `task-2-report.md`.
- Task 2: first implementer exited before edits/report because its model usage quota was exhausted; worktree remained clean. Recovery: fresh implementer with the same brief and a different available model.
- Task 2: recovery dispatch `/root/plan2_task2_retry` from unchanged base `70121b92cc64ba68aeef17bf8b580643ab53924d`.
- Task 2: recovery implementer was interrupted mid-task by a user turn and left uncommitted partial files with no report/commit. Fresh owner `/root/plan2_task2_resume` was dispatched to preserve, audit, and finish those edits from the same base.
- Task 2: implementation commit `ff500c556e9c9d59a608d4fcea8fb9d6b8e009e9`; local DB reset, 58 unit tests, 12 security tests, build, typecheck, lint, and secret scan reported PASS; review dispatched to `/root/plan2_task2_review` with `review-70121b9..ff500c5.diff`.
- Task 2: review found 4 Important gaps: OAuth-state purge has no production caller; worker runtime does not assume the scoped `service_worker` role; `@real2/integrations` does not declare its Zod runtime dependency; local Compose cannot satisfy the new mandatory OAuth env schema while disabled.
- Task 2: minor (deferred): RLS tests do not yet cover head/manager/anon status denial and exact service_worker table scope.
- Task 2: minor (deferred): AES-GCM tests do not explicitly tamper with IV/tag/ciphertext to prove authentication failure.
- Task 2: fix round 1/5 dispatched to original owner `/root/plan2_task2_resume` (0 addressed, 4 open; fix base `ff500c5`).
- Task 2: fix round 1/5 (4 addressed, 0 open; commit `09eb0f5`); scoped re-review PASS with no new breakage.
- Task 2: complete (commits `70121b9..09eb0f5`, review clean after fix round 1; 2 minors deferred).
- Task 2: controller verification after local Colima restart and synthetic Supabase reset: migrations 0001/0002 PASS; repository/worker tests 12/12 PASS; unit 58/58 PASS; security 13/13 PASS; build, Compose config, secret scan, diff check PASS; worktree clean.
- Task 3: dispatched to `/root/plan2_task3_impl` from base `09eb0f5f10f16766e9bde781afdb64d70847eccd`; brief `task-3-brief.md`; report `task-3-report.md`.
- Task 3: first implementer exhausted its model quota after leaving partial uncommitted route/service edits and no report. Recovery: fresh owner must preserve and audit the partial diff from the same base.
- Task 3: recovery dispatch `/root/plan2_task3_resume` from unchanged base `09eb0f5f10f16766e9bde781afdb64d70847eccd`.
- Task 3: implementation commit `45993c3d7f98b95d77f031740a9ff5724fe4265d`; focused integration 28, security 13, unit 58, repo/worker 12, lint, typecheck, build, and secret scan reported PASS; review dispatched to `/root/plan2_task3_review` with `review-09eb0f5..45993c3.diff`.
- Task 3: review found 4 Important gaps: start/callback redirects to an unimplemented parent page; “Проверить чтение” only refreshes OAuth and falsely advances the check time; UI does not reload status on failure and enables invalid actions; audit-sink mapping is duplicated across routes.
- Task 3: controller confirmed an additional load-bearing gap: `AMO_ACCOUNT_ID = 555151` infers the numeric API account ID from the subdomain, but the spec approves only host/subdomain and requires the API-returned account ID to become the stored binding; no numeric production account ID has been discovered or approved.
- Task 3: fix round 1/5 dispatched to original owner `/root/plan2_task3_resume` (0 addressed, 5 open; fix base `45993c3`).
- Task 3: fix round 1/5 (4 addressed, 1 open; commit `647544c`); re-review found refreshed tokens are committed before the account-binding GET, so a mismatched token pair can remain stored as `active`.
- Task 3: minor (deferred): client component tests assert disabled-state rendering but do not exercise safe-status reload after a mutation failure.
- Task 3: fix round 2/5 dispatched to original owner `/root/plan2_task3_resume` (0 newly addressed, 1 open; fix base `647544c`).
- Task 3: fix round 2/5 (1 addressed, 0 open; commit `286ef84`); scoped re-review PASS with no new breakage.
- Task 3: complete (commits `09eb0f5..286ef84`, review clean after 2 fix rounds; 1 minor deferred).
- Task 3: controller verification on synthetic Supabase after migrations 0001–0003: lint/typecheck PASS; repo/worker 12/12; unit 60/60; integration 30/30; security 13/13; build and secret scan PASS; worktree clean.
- Ruling: Plan Task 3 legitimately introduced `0003_amo_read_check.sql`, so Task 4 uses `0004_amo_configuration.sql` and Task 5 will use `0005_raw_sync.sql` instead of reusing occupied sequence numbers — ordered immutable migration history is safer than renaming a reviewed commit — if wrong, deployment tooling may need a documented migration-name remap before first production deploy.
- Task 4: dispatched to `/root/plan2_task4_impl` from base `286ef84651949ca194ea5935c276400a8568b6e9`; brief `task-4-brief.md`; report `task-4-report.md`.
- Task 4: first implementer exhausted its model quota after leaving partial uncommitted migration/domain/repository/API edits and no report. Recovery: fresh owner must preserve and audit the partial diff from the same base.
- Task 4: recovery dispatch `/root/plan2_task4_resume` from unchanged base `286ef84651949ca194ea5935c276400a8568b6e9`.
- Task 4: implementation commit `6c81fe95dcacde993b9c30ee10bcca26cd4cad4a`; synthetic Supabase reset, unit 68, integration 39, security 15, lint/typecheck/build/contracts/secret scan reported PASS; review dispatched to `/root/plan2_task4_review` with `review-286ef84..6c81fe9.diff`.
- Task 4: review found 4 Important gaps: same-checksum concurrent activations can both succeed; rename-only metadata drift has no warning/confirmation path; channel-values/mapping UI is incomplete; duplicate discovery IDs are accepted ambiguously.
- Task 4: Ruling: the encountered-value/count half of `/api/config/channel-values` is deferred to Task 5 because the immutable raw lead source does not exist until that task; Task 4 must still deliver editable exact rules/current mappings without fabricating counts, and Task 5 must close the raw aggregate contract — the spec requires truthful raw-derived counts — if wrong, Task 4 will need an earlier raw-value table and migration split.
- Task 4: minor (deferred): expand RLS/route negative coverage for child config rows and manager/anon access.
- Task 4: minor (deferred): render validation `checkedAt` and the fresh comparison checksum on `/quality/config`.
- Task 4: fix round 1/5 dispatched to original owner `/root/plan2_task4_resume` (0 addressed, 4 open with raw-count portion ruled to Task 5; fix base `6c81fe9`).
- Task 4: fix round 1/5 (3 addressed, 1 open; commit `61e2b5e`); reverse same-ID rename drift can still bypass explicit confirmation when a renamed stage returns to the original canonical name.
- Task 4: minor (deferred): rule editor permits local edits/removal of mandatory catalog mappings that the server later rejects, creating a misleading workflow although integrity remains protected.
- Task 4: fix round 2/5 dispatched to original owner `/root/plan2_task4_resume` (0 newly addressed, 1 open; fix base `61e2b5e`).
- Task 4: fix round 2/5 (1 addressed, 0 open; commit `689edae`); scoped re-review PASS with no new breakage.
- Task 4: complete (commits `286ef84..689edae`, review clean after 2 fix rounds; encountered channel counts ruled to Task 5; 3 minors deferred).
- Task 4: controller verification on synthetic PostgreSQL 17 after migrations 0001–0004: lint/typecheck PASS; repo/worker 12/12; unit 76/76; contracts command PASS; integration 44/44; security 15/15; build, secret scan, diff check PASS; worktree clean.
- Ruling: Task 5 adds a minimal append-only `sync_pages` metadata relation (run, stream, page number, item count, checksum, received time, no URL/query/body duplication) — the task title/interface and M4 auditor story require durable page evidence although the illustrative SQL omitted a page table — if wrong, one extra immutable metadata table will need removal before production.
- Task 5: dispatched to `/root/plan2_task5_impl` from base `689edae39b7c1910f6d0a78959dd8ea34114fb92`; brief `task-5-brief.md`; report `task-5-report.md`.
- Task 5: initial implementer was interrupted after leaving a partial migration/test-reset diff with no report or commit; fresh owner must preserve and audit the diff, complete the repositories/schemas/tests, and use migration `0005_raw_sync.sql`.
- Task 5: implementation commit `9b4ab150c89069dd90a7843d80218b3ede98bb3a` (`feat: persist append-only amoCRM sync journal`); report `task-5-report.md`; controller evidence from the owner: migrations 0001–0005 reset cleanly, repo/worker 12/12, unit 80/80, integration 52/52, security 17/17, lint/typecheck/contracts/build/secret scan/diff check PASS; no live amoCRM/OAuth/Google activity.
- Task 5: review dispatched to `/root/plan2_task5_review` with `review-689edae..9b4ab15.diff`.
- Task 5: review FAIL with 4 Important findings and 2 Minors. Important: event IDs are string-shaped in the official amoCRM Events API but schema/storage require numeric IDs; valid object-valued/chained-list lead custom fields are rejected; service_worker can bypass append-only sealing by appending/rewriting terminal runs and advancing cursors outside the repository transaction; durable audit paths can retain dynamic lead IDs/PII. Minors: channel-value grouping trims/collapses exact raw values; event-conflict/RLS coverage is incomplete. Reviewer found no amoCRM/Google mutations. Fix round 1/5 is required; same owner must preserve the commit and address all blockers, including a reviewed correction to the affected M4/spec contract.
- Task 5: fix round 1/5 original owner lost authentication after leaving 12 uncommitted modified files (spec/plan corrections, migration, repositories, schemas, integration/security tests) and no commit/report update. Recovery owner must preserve and audit the partial diff, remove any unearned owner-approval claim, complete verification, then commit the same fix round.
- Ruling: the event-ID correction is a technical conformance amendment verified against the official amoCRM API, not a new business-rule approval by the project owner — the dedupe formula is unchanged, but the approval ledger must not claim user approval that did not occur — if wrong, the owner can explicitly ratify version 1.0.2 later without changing code.
- Task 5: fix round 1/5 recovery owner completed focused raw-journal 16/16 and schema 10/10 checks plus lint/typecheck, including both append/finalize lock orders and sealing probes, then hit its model usage limit before the full suite/report/commit. The 12-file partial diff remains preserved; a fresh recovery owner must audit it, complete full verification, report, and commit.
- Task 5: fix round 1/5 completed by fresh recovery owner (all 4 Important and both Minor findings addressed; commit `30aa40cde8525fa69513af3311f820374c841ce7`). Node 22.23.2 evidence: migrations 0001–0005 reset PASS; repo/worker 12/12; unit 86/86; integration 61/61; security 18/18; contracts/lint/typecheck/build/secret scan/diff check PASS; worktree clean; no live amoCRM/OAuth/Google activity.
- Task 5: scoped re-review dispatched against `review-9b4ab15..30aa40c.diff`.
- Task 5: scoped re-review PASS; all 4 Important and both Minor findings addressed with no new findings. Reviewer independently reset local DB and observed schema 10/10, raw 16/16, integration 61/61, security 18/18, unit 86/86, repo/worker 12/12 plus typecheck/secret scan/diff check PASS under Node 22; no live side effects.
- Task 5: complete (commits `689edae..30aa40c`, review clean after fix round 1; event-ID contract corrected as a review-verified technical amendment with no new owner approval).
- Task 5: controller verification on Node 22.23.2 after synthetic Supabase reset and migrations 0001–0005: repo/worker 12/12, unit 86/86, integration 61/61, security 18/18, contracts command, lint, typecheck, build, secret scan, and diff check PASS; worktree clean at `30aa40c`.
- Task 6: dispatched from base `30aa40cde8525fa69513af3311f820374c841ce7`; brief `task-6-brief.md`; report `task-6-report.md`.
- Task 6: initial owner hit its model usage limit after leaving a large uncommitted worker/DB/API/testkit diff and no report/commit. Reported green evidence before interruption: focused unit sync/schedule/watchdog/retention/mock tests; migration 0006 reset; DB integration 4/4 for session lock, cursors/counts, watchdog sealing, and retention proof; transport 11/11. Fresh recovery owner must preserve/audit all tracked and untracked changes, finish API/UI wiring, run the complete matrix, report, and commit.
- Task 6: recovery completed commit `bde1c9a` (`feat: sync amoCRM through guarded read-only worker`) and report `task-6-report.md`; Node 22.23.2 evidence: migrations 0001–0006, focused 29/29, repo/worker 12/12, unit 99/99, integration 73/73, security 18/18, contracts/lint/typecheck/build/secret scan/diff check PASS; switches remained false and no live external activity occurred.
- Task 6: review dispatched with `review-30aa40c..bde1c9a.diff`.
- Task 6: review FAIL with 8 Important and 3 Minor findings. Important: retention proof is synthetic/unbound and cutoff is caller-controlled; schedule/watchdog/retention have no production caller; advisory-lock session loss does not fence an active run and cleanup can leak; cross-page duplicate entities with changing envelope metadata bypass logical loop and distinct count-drop detection; ordinary worker DB login can `SET ROLE retention_worker`; count-drop blocking creates no durable critical alert; failures after run start but before the main try leave a running row; manual HTTP POST awaits the full sync instead of durably acknowledging queued work. Minors: sync UI dates omit Moscow timezone, history UI has no pagination, manual initiator is not persisted. Focused reviewer tests/probes otherwise confirmed read-only transport, double-switch gate, retry/overlap/quarantine paths. Fix round 1/5 required.
- Ruling: because Plan 3 normalized relations do not yet exist, Task 6 retention must fail closed and delete zero raw rows until it can verify a concrete normalized row and matching retained hash; an arbitrary proof string is not acceptable — this preserves M4 evidence and lets Plan 3 attach real normalization evidence — if wrong, raw storage will grow temporarily rather than risk premature deletion.
- Task 6: fix round 1/5 partial commit `3059a95` (`fix: harden guarded sync operations`). Owner reports retention fail-closed/cutoff and role isolation, durable manual queue/requested_by, lifecycle terminalization, logical page loop/distinct count, critical alerts, and non-placeholder five-minute/watchdog entrypoint; Node 22 migrations through 0007, repo/worker 12/12, unit 99/99, integration 72/72 PASS. Owner explicitly left advisory-session fencing, complete queue/nightly/retention worker wiring, preflight lock conflict, and UI timezone/pagination open. Scoped re-review dispatched with `review-bde1c9a..3059a95.diff` before round 2.
- Task 6: fix round 1/5 re-review FAIL. Addressed: fail-closed 90-day retention, logical duplicate/distinct-count gate, normal critical alert, run initialization sealing, asynchronous queue acknowledgement. Open/partial: recurring schedule/nightly/watchdog/retention wiring; advisory-session fencing/cleanup; worker-vs-retention credential isolation; durable queue consumer/crash recovery/requester propagation; busy-lock 409; Moscow UI/pagination. New Important regressions: alert sink failure can prevent terminalization; partial/count-drop outcomes lose persisted lead counts/checksum diagnostics. Report contains stale pre-fix claims. Fix round 2/5 required on exact reviewer list.
- Task 6: fix round 2/5 resume attempt failed immediately on model usage limit with no edits; worktree remains clean at `3059a95`. Fresh recovery owner must execute the same exact round-2 list.
- Task 6: fix round 2/5 completed by fresh recovery owner; commit `37d240ab80c59e4b7756a05acf4b06dc75cc6624` (`fix: harden guarded sync worker`). Report rewritten. Node 22 evidence: migrations 0001–0008; focused unit/UI 11; focused DB/API 13 including backend-termination fencing; repo/worker TAP 15 in full test plus 105 unit; integration 77/77; security 18/18; contracts/lint/typecheck/build/secret/diff PASS; retention remains zero-delete; switches false/no live activity.
- Task 6: scoped re-review round 2 dispatched with `review-3059a95..37d240a.diff`.
- Task 6: round-2 reviewer hit its model usage limit before final verdict. Preliminary open Important candidates: queue retry reuses a `trace_id` that conflicts with unique `sync_runs.trace_id`; exhausted attempts may remain unclaimable forever; queue processor may mark partial/failed sync results `done`; exact-minute inline scheduler may miss 02:30 catch-up; fencing may not be rechecked immediately before transport/proactive refresh. Fresh reviewer must complete the same scoped re-review and preserve these candidates.
- Task 6: fresh round-2 re-review FAIL confirmed 5 Important findings: crash-reclaimed queue work reuses the queue trace against unique `sync_runs.trace_id`; max-attempt expired rows remain non-terminal; partial/failed sync results are marked queue `done`; lock fencing does not cover proactive token refresh/account validation/token rotation; nightly reconciliation is lost after the exact 02:30 Moscow minute. One Minor: UI/report overstate initiator attribution because createdBy is not rendered. Core round-2 suites otherwise passed. Fix round 3/5 required.
- Task 6: fix round 3/5 completed under controller takeover after the implementation agent stalled during handoff; commit `cc7ec708cbfdc26ac4453770643843b84e292bc9` (`fix: finalize guarded sync recovery`). Closed the 5 Important findings and UI attribution: stable queue correlation plus unique attempt traces, exhausted-job sweep, truthful queue terminal mapping, OAuth refresh/network/token-rotation fencing, Moscow nightly catch-up, createdBy rendering. Controller verification on Node 22.23.2: migrations 0001–0009; focused unit 31, focused DB/API 15, focused worker 12, full repo/worker 16, unit 110, integration 80, security 18, build/lint/typecheck/contracts/secret/diff PASS; no live external activity and switches remain false.
- Task 6: complete (commits `30aa40c..cc7ec70`; all reviewer findings through round 3 addressed).
- Task 7: implemented from clean base `cc7ec70` with two static security gates, reusable source scanner, external OAuth installation runbook, and emergency shutdown runbook. TDD RED was observed on Node 22.23.2 because `listTypeScriptFiles` was missing; GREEN security result is 20/20.
- Task 7: full controller gate on Node 22.23.2: repo/worker 16/16, unit 110/110, contracts command PASS, integration 80/80, security 20/20, lint, typecheck, build, secret scan, and diff check PASS. No live OAuth, amoCRM, Google API, or source-sheet activity occurred; both switch defaults remain false.
