# Restore REAL2 Repository

Code and memory now live in one repository. Clone it directly:

```text
git clone https://github.com/sarrinoj-glitch/REAL2-Sales-Dashboard.git
cd REAL2-Sales-Dashboard
git checkout feat/foundation-access
```

`project-memory/` arrives with the checkout. No nested repository, source snapshot, or bundle is required.
